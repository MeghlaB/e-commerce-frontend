import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images:{
    domains: ['images.unsplash.com','th.bing.com', 'i.ibb.co','example.com'],
  }
};

export default nextConfig;
